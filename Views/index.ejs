<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8" />
    <title><%= clan.name %> | opsucht.net Clan</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="bg-overlay"></div>

    <header>
        <div class="logo">
            <span class="vh">VH</span>
            <span class="name"><%= clan.name %></span>
        </div>
        <nav>
            <a href="#about">Über uns</a>
            <a href="#roster">Roster</a>
            <a href="#requirements">Bewerbung</a>
            <a href="#contact">Kontakt</a>
        </nav>
    </header>

    <main>
        <section class="hero">
            <div class="hero-content">
                <h1><%= clan.name %></h1>
                <h2>Clan auf <span class="highlight"><%= clan.network %></span></h2>
                <p><%= clan.tagline %></p>
                <div class="hero-buttons">
                    <a href="#requirements" class="btn primary">Jetzt bewerben</a>
                    <a href="#roster" class="btn secondary">Roster ansehen</a>
                </div>
            </div>
        </section>

        <section id="about" class="card">
            <h3>Über Void Hunters</h3>
            <p><%= clan.description %></p>
            <div class="tags">
                <% clan.focus.forEach(mode => { %>
                    <span class="tag"><%= mode %></span>
                <% }) %>
            </div>
        </section>

        <section id="roster" class="card">
            <h3>Roster</h3>
            <div class="roster-grid">
                <% clan.roster.forEach(player => { %>
                    <div class="player-card">
                        <div class="player-avatar"><%= player.name[0] %></div>
                        <div class="player-info">
                            <h4><%= player.name %></h4>
                            <span class="role"><%= player.role %></span>
                            <p><%= player.note %></p>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>

        <section id="requirements" class="card">
            <h3>Bewerbung & Anforderungen</h3>
            <% if (clan.recruitmentOpen) { %>
                <p>Wir suchen aktuell aktive Spieler, die Bock auf ernstes, aber entspanntes Competitive haben.</p>
            <% } else { %>
                <p>Aktuell sind die Bewerbungen geschlossen. Schau später nochmal vorbei.</p>
            <% } %>

            <ul class="requirements">
                <% clan.requirements.forEach(req => { %>
                    <li>• <%= req %></li>
                <% }) %>
            </ul>

            <div class="apply-box">
                <p>Bewerbung über unseren Discord:</p>
                <a href="<%= clan.socials.discord %>" class="btn primary" target="_blank">Discord joinen</a>
            </div>
        </section>

        <section id="contact" class="card">
            <h3>Kontakt & Links</h3>
            <div class="links">
                <a href="<%= clan.socials.discord %>" target="_blank">Discord</a>
                <a href="<%= clan.socials.opsuchtProfile %>" target="_blank">opsucht.net</a>
                <% if (clan.socials.youtube !== "#") { %>
                    <a href="<%= clan.socials.youtube %>" target="_blank">YouTube</a>
                <% } %>
                <% if (clan.socials.twitch !== "#") { %>
                    <a href="<%= clan.socials.twitch %>" target="_blank">Twitch</a>
                <% } %>
            </div>
        </section>
    </main>

    <footer>
        <span>Void Hunters • opsucht.net • <%= new Date().getFullYear() %></span>
    </footer>
</body>
</html>
